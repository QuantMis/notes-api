{
  "name": "notes-app",
  "private": true,
  "workspaces": [
    "infra",
    "functions/*"
  ],
  "scripts": {
    "test": "npm --workspaces --if-present run test"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^6.0.0",
    "jest": "^29.7.0"
  }
}
